--- a/src/pcresearch.c
+++ b/src/pcresearch.c
@@ -129,7 +129,7 @@
 
 #if defined HAVE_LANGINFO_CODESET
   if (STREQ (nl_langinfo (CODESET), "UTF-8"))
-    flags |= PCRE_UTF8;
+    flags |= PCRE_NO_UTF8_CHECK;
 #endif
 
   if (!map_pcre ())
